import 'package:flutter/material.dart';

class Sector {
  final String name;
  final double percent;
  final Color color;

  Sector({
    required this.name,
    required this.percent,
    required this.color,
  });
}